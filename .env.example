# Match credentials in docker-compose.yml (POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB).
# Local Postgres (Docker): postgresql://app:app@localhost:5432/todos
DATABASE_URL=postgresql://app:app@localhost:5432/todos
